'use client';

import { createTheme } from '@mantine/core';

// Define your base theme override or leave empty for defaults
export const theme = createTheme({
  /* Put your mantine theme override here */
  // Example: fontFamily: 'Open Sans, sans-serif',
  // Example: primaryColor: 'cyan',
});
